import React from "react";
import {withProviders} from "./providers";

function App() {
    return (
        <div className="App">
            GOOD JOB!
        </div>
    );
}

export default withProviders(App);

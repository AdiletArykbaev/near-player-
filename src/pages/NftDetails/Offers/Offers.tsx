import React from 'react';
import "./Offers.scss"

const Offers = () => {
    return (
        <div className="Offers">
                <button className="Button">
                    No offers received
                </button>
        </div>
    );
};

export default Offers;